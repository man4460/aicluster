/**
 * คำสั่งเดียวกันสำหรับ: Telegram caption (Mavel) / Ollama vision / buildPrompt อ้างอิง
 */

/** โมเดล Ollama ดีฟอลต์สำหรับอ่านรูปสลิป (vision) — แทน llava */
export const OLLAMA_DEFAULT_SLIP_VISION_MODEL = "qwen2.5vl:7b";

export const SLIP_ANALYSIS_CHECKLIST_LINES = [
  "วันที่และเวลา (ตามที่ปรากฏบนสลิป)",
  "จำนวนเงิน/ยอดโอน (บาท)",
  "รหัสอ้างอิง (ref/เลขที่รายการ)",
  "ชื่อผู้โอนและผู้รับ (หรือเลขบัญชี/พร้อมเพย์)",
  "ธนาคาร/ช่องทางและสถานะการทำรายการ",
] as const;

const checklistMarkdown = SLIP_ANALYSIS_CHECKLIST_LINES.map((line) => `- ${line}`).join("\n");

/** รายละเอียดหัวข้อ 4 (ใช้ใน Ollama / อ้างอิง) */
export const SLIP_POST_READ_STEPS = [
  "สรุปรายการที่อ่านได้ เรียงตามรายการด้านบน",
  "ระบุข้อใดอ่านไม่ชัด/อ่านไม่ได้ หรือยังอ่านไม่ครบ",
  "ดำเนินการตอบเชิงโครงสร้างตามคำสั่งถัดไป (เช่น รูปแบบ JSON) — สำหรับ field ใดอ่านไม่ได้ ให้ใส่ null หรืออธิบาย",
  "ส่งผลกลับไปที่ Chat UI ให้ผู้ใช้ที่ส่งรูปนี้ (หรือตามช่องทางที่มอบหมาย)",
  "ถามผู้ใช้ว่าต้องการบันทึก (ลงรายการ) หรือไม่ — อย่าบังคับบันทึกเองก่อนได้รับคำตอบ",
] as const;

const postReadMarkdown = SLIP_POST_READ_STEPS.map((line) => `- ${line}`).join("\n");

const slipOcrThaiGuidelines = [
  "อ่านตามลำดับบรรทัดบนสลิป แยก “หัวข้อ/คำอธิบาย” กับ “ค่า” ให้ชัด — ไล่จากบนลงล่างหรือตามดีไซน์ของแอปธนาคาร/พร้อมเพย์",
  "ตัวเลขเงิน: อาจมีเครื่องหมายคอมมาคั่นหลักพัน ทศนิยม หรือคำว่า บาท/สตางค์ — คัดลอกตามที่มองเห็น ห้ามปัดเศษหรือเดายอดเอง",
  "วันที่และเวลา: รักษารูปแบบตามสลิป (พ.ศ./ค.ศ. เวลา 24 ชม. ฯลฯ) ถ้าแปลงปีได้อย่างมั่นใจเท่านั้น มิฉะนั้นใส่ตามที่อ่านได้",
  "ชื่อบุคคล/นามบัญชี/ร้านค้า: พิมพ์ตามตัวอักษรที่อ่านได้ ห้ามเปลี่ยนสะกดหรือเดาชื่อเต็มถ้าอ่านไม่ครบ",
  "ถ้าภาพเบลอ แสงสะท้อน เงา มุมเอียง หรือตัดขอบ — อ่านเฉพาะส่วนที่มั่นใจ และระบุว่าส่วนใดอ่านไม่ชัด",
  "ห้ามสร้าง แก้ไข หรือเดาข้อมูลที่มองไม่เห็นในภาพ (ยอดเงิน รหัส ชื่อ ฯลฯ)",
].map((line) => `- ${line}`).join("\n");

/** บล็อกหัว — ใช้นำหน้า prompt ของ Ollama เมื่อมีรูปสลิป */
export const THAI_SLIP_VISION_OCR_BLOCK = `คุณช่วยอ่านข้อความและตัวเลขจากภาพสลิป/หลักฐานการโอนเงิน หรือบิลภาษาไทย โดยเน้นความตรงกับที่มองเห็น (OCR) มากกว่าการตีความ

รูปอาจเป็นสลิปแอปธนาคาร พร้อมเพย์ หรือรูปหน้าจอ — อ่านทุกบรรทัดที่อ่านได้

สิ่งที่ต้องพยายามดึงออกมา (ถ้ามีในภาพ):
${checklistMarkdown}

กฎการอ่าน (สำคัญ):
${slipOcrThaiGuidelines}

4. เมื่ออ่านเสร็จ:
${postReadMarkdown}`.trim();

/** สั้นๆ สำหรับแคปชัน Telegram / อ้างอิง */
export const SLIP_UNCERTAINTY_RULE_LINE =
  "อ่านไม่ชัดหรือไม่แน่ใจ ให้บอกตรง ๆ ห้ามเดาหรือเติมตัวเลข/ชื่อเอง";

/** หัวข้อ 4 ฉบับย่อ สำหรับแคปชัน (จำกัด 1024 ตัวอักษร) */
export const SLIP_POST_READ_CAPTION_LINE =
  "4. เมื่ออ่านเสร็จ: สรุปรายการที่อ่านได้, บอกจุดที่อ่านไม่ชัด, ดำเนินการตอบ/ส่งตามรูปแบบ, ส่งผลกลับ Chat UI, ถามบันทึกหรือไม่";

/** แคปชัน: หัวข้อกลุ่มสรุปงาน / ลำดับขั้นสำหรับ Mavel */
export const SLIP_CAPTION_COMMANDS_SUMMARY_HEADER = "📝 สรุปคำสั่ง:";

/** แคปชัน: บอก Mavel ให้ส่งผลกลับฝั่งเว็บ */
export const SLIP_SEND_RESULT_TO_CHAT_UI_LINE = "• ส่งผลกลับไปที่ Chat UI";

/** แคปชัน: บอก Mavel ให้ถามผู้ใช้ก่อนบันทึก */
export const SLIP_ASK_WHETHER_TO_SAVE_LINE = "• ถามว่าต้องการบันทึกหรือไม่";

export function withThaiSlipOcrPreamble(visionText: string, rest: string): string {
  const t = rest.trim();
  if (!t) return visionText;
  return `${visionText}

---

${t}`;
}
