/**
 * ลิงก์สาธารณะร้านออนไลน์ / พนักงาน — เต็มจอ
 */
export default function EcommerceStorePublicLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-[100dvh] w-full">{children}</div>;
}
