import { MediaRegistryBorrowClient } from "@/systems/media-registry/components/MediaRegistryBorrowClient";

export default function MediaRegistryBorrowPage() {
  return <MediaRegistryBorrowClient />;
}
